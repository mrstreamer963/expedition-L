# Tasks: Hello World with Vite + React

## 1. Initialize the project
- [x] Create `package.json` with project metadata
- [x] Add runtime dependencies: `react`, `react-dom`
- [x] Add development dependencies: `vite`, `@vitejs/plugin-react`, `typescript`, `@types/react`, `@types/react-dom`

## 2. Configure the build tooling
- [x] Create `vite.config.ts` with React plugin
- [x] Create `tsconfig.json` for the application code
- [x] Create `tsconfig.node.json` for Node-side configuration
- [x] Create `index.html` as the HTML entry template

## 3. Set up the application source
- [x] Create `src/vite-env.d.ts` with Vite client type reference
- [x] Create `src/index.css` with minimal global styles
- [x] Create `src/App.tsx` with the Hello World component
- [x] Create `src/main.tsx` as the entry point that renders App

## 4. Add assets and finalize
- [x] Create `public/` directory
- [x] Add a basic favicon or placeholder in `public/`

## 5. Verify the setup
- [x] Run `npm run dev` and confirm the dev server starts
- [x] Open the browser and verify "Hello, World!" is displayed
- [x] Run `npm run build` and confirm a successful production build
- [x] Run `npm run preview` and verify the production build serves correctly
