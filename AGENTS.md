## Git

- Always use `git add -A` (or `git add --all`) before committing to capture all changes — deletions, additions, and modifications.
- `git mv` is not necessary; `mv + git add -A` handles renames properly.
